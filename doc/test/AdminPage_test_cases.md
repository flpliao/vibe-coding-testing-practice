# AdminPage 測試案例

> 狀態：初始為 [ ]、完成為 [x]
> 注意：狀態只能在測試通過後由流程更新。
> 測試類型：前端元素、function 邏輯、Mock API、驗證權限...

---

## [x] 【前端元素】檢查 Admin 頁面元素是否正確渲染
**範例輸入**：
1. Mock User role 為 'admin'
2. 進入 Admin 頁面
**期待輸出**：
1. 看到「管理後台」標題
2. 看到「管理員專屬頁面」卡片
3. 看到「登出」按鈕
4. 看到 role badge 顯示「管理員」

---

## [x] 【驗證權限】測試 Admin 權限訪問
**範例輸入**：
1. Mock User role 為 'admin'
2. 進入 Admin 頁面
**期待輸出**：正常渲染 Admin 頁面內容

---

## [x] 【驗證權限】測試非 Admin 權限訪問 (UI顯示)
**範例輸入**：
1. Mock User role 為 'user'
2. 進入 Admin 頁面
**期待輸出**：顯示「一般用戶」Badge (雖然正常情況下會被 Route 擋下，但組件本身會依據 user 渲染)

---

## [x] 【function 邏輯】測試登出功能
**範例輸入**：點擊「登出」按鈕
**期待輸出**：
1. 呼叫 logout 方法
2. 導向至 `/login`

---

## [x] 【前端元素】檢查返回連結
**範例輸入**：點擊「返回」連結
**期待輸出**：導向至 `/dashboard`

---
